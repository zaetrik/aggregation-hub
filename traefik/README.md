Traefik Reverse Proxy for routing requests to the docker services.

Certificates:

    In development:
        - create certs with mkcert utility and add them to the certs/ folder (_wildcard.localhost-key.pem & _wildcard.localhost.pem)
